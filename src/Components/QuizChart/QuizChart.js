import React from "react";

const QuizChart = ({ info }) => {
  const { name, total } = info;
  console.log(name);

  return (
    <div>
      <h1>This is form quiz chart real</h1>
    </div>
  );
};

export default QuizChart;
